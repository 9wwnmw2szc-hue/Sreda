import {before,after,test} from 'node:test';import assert from 'node:assert/strict';import {randomUUID} from 'node:crypto';import {Kysely,PGliteDialect} from 'kysely';import {PGlite} from '@electric-sql/pglite';import {migrate} from '../src/server/db/migrate.ts';import {PostService} from '../src/server/posts/service.ts';import {queueScheduledPost,materializeRecurringPost} from '../src/server/posts/worker.ts';import {TelegramService} from '../src/server/telegram/service.ts';import {VKService} from '../src/server/vk/service.ts';import {encryptSecret} from '../src/server/connections/crypto.ts';
const db=new Kysely({dialect:new PGliteDialect({pglite:new PGlite()})});const secret='fixture-secret-'.repeat(4);before(()=>migrate(db,new URL('../migrations',import.meta.url).pathname));after(()=>db.destroy());
async function fixture(){const uid=randomUUID();await db.insertInto('user').values({id:uid,name:'Admin',email:uid+'@test.invalid',emailVerified:false,username:'u'+uid}).execute();const b=await db.insertInto('business').values({id:randomUUID(),name:'Posts business',timezone:'Europe/Kaliningrad'}).returningAll().executeTakeFirstOrThrow();await db.insertInto('business_member').values({business_id:b.id,user_id:uid,role:'owner',status:'active'}).execute();await db.insertInto('business_solution').values({business_id:b.id,solution_code:'autopost',status:'active',starts_at:new Date(),expires_at:null}).execute();const targets=[];for(const platform of ['telegram','vk']){const connection_id=randomUUID();await db.insertInto('business_connection').values({id:connection_id,business_id:b.id,platform,external_account_id:randomUUID(),display_name:'fixture',status:'connected'}).execute();await db.insertInto('connection_secret').values({connection_id,encrypted_token:encryptSecret('fixture-token',secret),encrypted_publish_token:encryptSecret('fixture-user-token',secret),key_version:1}).execute();await db.insertInto(platform+'_runtime').values({connection_id,generation:randomUUID(),status:'ready'}).execute();const target={id:randomUUID(),business_id:b.id,connection_id,platform,external_id:platform==='telegram'?'-100123':'-123',title:platform};await db.insertInto('post_target').values(target).execute();targets.push(target.id);}return {uid,b,targets,svc:new PostService(db,secret)};}
test('scheduled post reaches both outboxes; partial failure retries only failed platform',async()=>{const f=await fixture();const body={text:'Новая услуга',targets:f.targets,action:'now',request_key:randomUUID()};const p=await f.svc.save(f.uid,f.b.public_id,body);assert.equal((await f.svc.save(f.uid,f.b.public_id,body)).id,p.id);await queueScheduledPost(db);let tgCalls=0;const tg=new TelegramService(db,secret,'https://fixture.test',true,async()=>{tgCalls++;return Response.json({ok:true,result:{message_id:555}});});const vkFail=new VKService(db,secret,true,undefined,async()=>Response.json({error:{error_code:15}}));await tg.deliverOne();await vkFail.deliverOne();let posts=await f.svc.list(f.uid,f.b.public_id);assert.equal(posts[0].status,'partial');await f.svc.action(f.uid,f.b.public_id,p.id,'retry');await queueScheduledPost(db);const vk=new VKService(db,secret,true,undefined,async(url,init)=>{const params=new URLSearchParams(init.body);assert.equal(url.split('/').at(-1),'wall.post');assert.ok(params.get('guid'));return Response.json({response:{post_id:777}});});await vk.deliverOne();await tg.deliverOne();posts=await f.svc.list(f.uid,f.b.public_id);assert.equal(posts[0].status,'published');assert.equal(tgCalls,1);assert.ok(posts[0].deliveries.every(d=>d.external_message_id));});
test('recurring job is materialized once and cancelling leaves published history',async()=>{const f=await fixture();const date=new Date(Date.now()+86400000).toISOString().slice(0,10);const template=await f.svc.save(f.uid,f.b.public_id,{text:'Повтор',targets:f.targets,action:'schedule',request_key:randomUUID(),recurrence:{frequency:'daily',start:date,time:'10:00'}});await db.updateTable('post_schedule').set({next_at:new Date(Date.now()-1000)}).where('post_id','=',template.id).execute();await materializeRecurringPost(db);await materializeRecurringPost(db);const occurrences=await db.selectFrom('post').selectAll().where('template_id','=',template.id).execute();assert.equal(occurrences.length,1);await db.updateTable('post').set({status:'published'}).where('id','=',occurrences[0].id).execute();await f.svc.action(f.uid,f.b.public_id,template.id,'cancel');assert.equal((await db.selectFrom('post').select('status').where('id','=',occurrences[0].id).executeTakeFirstOrThrow()).status,'published');});
test('temporary platform error uses bounded retry delay and unknown delivery cannot be retried',async()=>{const f=await fixture();const p=await f.svc.save(f.uid,f.b.public_id,{text:'Retry',targets:[f.targets[0]],action:'now',request_key:randomUUID()});await queueScheduledPost(db);const worker=new TelegramService(db,secret,'https://fixture.test',true,async()=>Response.json({ok:false},{status:429}));await worker.deliverOne();const delivery=await db.selectFrom('post_delivery').select('id').where('post_id','=',p.id).executeTakeFirstOrThrow();const row=await db.selectFrom('telegram_outbox').selectAll().where('post_delivery_id','=',delivery.id).executeTakeFirstOrThrow();assert.ok(+row.available_at-Date.now()>50000);assert.equal(row.delivery_state,'pending');await db.updateTable('telegram_outbox').set({available_at:new Date(0)}).where('id','=',row.id).execute();const uncertain=new TelegramService(db,secret,'https://fixture.test',true,async()=>{throw Error('timeout');});await uncertain.deliverOne();await assert.rejects(f.svc.action(f.uid,f.b.public_id,p.id,'retry'),e=>e.code==='DELIVERY_UNCERTAIN');});
