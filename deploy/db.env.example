# New self-hosted installs. Existing VMs that already initialized Postgres as
# user/db `sreda` must keep those names — do not rename a live volume.
POSTGRES_USER=biznesoty
POSTGRES_DB=biznesoty
POSTGRES_PASSWORD=REPLACE_WITH_SAME_HEX_PASSWORD
