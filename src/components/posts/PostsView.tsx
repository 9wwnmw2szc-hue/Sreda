"use client";
import {AttachmentPicker,type FileItem} from "@/components/attachments/AttachmentPicker";
import {useEffect,useRef,useState} from 'react';import {apiRequest} from '@/lib/apiClient';import {useBusinessContext} from '@/hooks/useBusinessContext';
type Target={id:string;title:string;platform:string;active:boolean};
type Post={attachments:FileItem[];id:string;text:string;buttons:{text:string;url:string}[];status:string;revision:number;scheduled_at:string|null;deliveries:{target_id:string;title:string;platform:string;status:string;last_error:string|null}[];schedule?:{active:boolean;rule:{frequency:string;start:string;time:string;end?:string;weekdays?:number[]}}};
const statuses:Record<string,string>={draft:'Черновик',scheduled:'Запланирован',publishing:'Отправляется',published:'Опубликован',partial:'Частично опубликован',failed:'Ошибка',cancelled:'Отменён',pending:'Ожидает',uncertain:'Доставка не подтверждена'};
export function PostsView(){const {currentBusiness}=useBusinessContext();if(!currentBusiness)return <p>Выберите бизнес.</p>;if(currentBusiness.role==='operator')return <p>Публикациями управляет владелец или администратор.</p>;return <Editor key={currentBusiness.id} businessId={currentBusiness.id} timezone={currentBusiness.timezone??'UTC'}/>;}
function Editor({businessId,timezone}:{businessId:string;timezone:string}){
 const [files,setFiles]=useState<FileItem[]>([]),[uploading,setUploading]=useState(false);
 const base=`/api/v1/businesses/${businessId}`;const [posts,setPosts]=useState<Post[]>([]),[targets,setTargets]=useState<Target[]>([]),[loaded,setLoaded]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false),[filter,setFilter]=useState('all'),[text,setText]=useState(''),[chosen,setChosen]=useState<string[]>([]),[buttons,setButtons]=useState<{text:string;url:string}[]>([]),[editing,setEditing]=useState<Post|null>(null),[date,setDate]=useState(''),[time,setTime]=useState('12:00'),[frequency,setFrequency]=useState(''),[end,setEnd]=useState(''),[weekdays,setWeekdays]=useState<number[]>([1]),[prompt,setPrompt]=useState(''),[platform,setPlatform]=useState('telegram'),[channel,setChannel]=useState(''),[publishToken,setPublishToken]=useState(''),[confirm,setConfirm]=useState<{post:Post;action:string}|null>(null);const key=useRef('');
 async function refresh(){const [p,t]=await Promise.all([apiRequest<Post[]>(base+'/posts'),apiRequest<Target[]>(base+'/post-targets')]);setPosts(p);setTargets(t);}
 useEffect(()=>{let active=true;async function load(){try{const [p,t]=await Promise.all([apiRequest<Post[]>(base+'/posts'),apiRequest<Target[]>(base+'/post-targets')]);if(active){setPosts(p);setTargets(t);setLoaded(true);}}catch(e){if(active){setError(e instanceof Error?e.message:'Не удалось загрузить.');setLoaded(true);}}}void load();const timer=setInterval(()=>void load(),10000);return()=>{active=false;clearInterval(timer);};},[base]);
 function reset(){setEditing(null);setText('');setFiles([]);setChosen([]);setButtons([]);setFrequency('');key.current='';}
 async function save(action:string){setBusy(true);setError('');key.current||=crypto.randomUUID();try{await apiRequest(base+'/posts'+(editing?'/'+editing.id:''),{method:editing?'PATCH':'POST',body:JSON.stringify({text,attachments:files.map(f=>f.id),targets:chosen,buttons,action,request_key:key.current,revision:editing?.revision,date,time,...(frequency&&action==='schedule'?{recurrence:{frequency,start:date,time,end:end||undefined,weekdays}}:{})})});reset();setNotice(action==='draft'?'Черновик сохранён.':'Публикация поставлена в очередь.');await refresh();}catch(e){setError(e instanceof Error?e.message:'Не удалось сохранить.');}finally{setBusy(false);}}
 async function action(post:Post,value:string){setBusy(true);setError('');try{await apiRequest(base+'/posts/'+post.id,{method:'POST',body:JSON.stringify({action:value})});setConfirm(null);await refresh();}catch(e){setError(e instanceof Error?e.message:'Не удалось выполнить действие.');}finally{setBusy(false);}}
 async function ai(action:string){setBusy(true);setError('');try{const result=await apiRequest<{text:string}>(base+'/posts/ai',{method:'POST',body:JSON.stringify({prompt,text,action})});setText(result.text);key.current='';setNotice('AI подготовил черновик. Проверьте текст перед публикацией.');}catch(e){setError(e instanceof Error?e.message:'Не удалось создать текст.');}finally{setBusy(false);}}
 async function connect(){setBusy(true);setError('');try{const token=publishToken;setPublishToken('');await apiRequest(base+'/post-targets',{method:'POST',body:JSON.stringify({platform,channel,publishToken:token})});await refresh();setNotice('Площадка проверена и подключена.');}catch(e){setError(e instanceof Error?e.message:'Не удалось подключить площадку.');}finally{setBusy(false);}}
 return <div><h1>Публикации</h1><p>Планирование в часовом поясе {timezone}. AI создаёт только черновики.</p>{error&&<p role="alert" className="account-error">{error}</p>}{notice&&<p role="status">{notice}</p>}<div className="crm-columns"><section className="panel crm-panel"><h2>{editing?'Редактировать публикацию':'Создать публикацию'}</h2>{targets.filter(t=>t.active).map(t=><label key={t.id}><input type="checkbox" checked={chosen.includes(t.id)} onChange={e=>{setChosen(e.target.checked?[...chosen,t.id]:chosen.filter(x=>x!==t.id));key.current='';}}/>{t.platform} · {t.title}</label>)}{loaded&&!targets.length&&<p>Подключите площадку ниже.</p>}<label>Текст<textarea maxLength={4096} value={text} onChange={e=>{setText(e.target.value);key.current='';}}/></label><AttachmentPicker businessId={businessId} files={files} onChange={v=>{setFiles(v);key.current="";}} mediaOnly disabled={busy} onBusy={setUploading}/><details><summary>Создать с AI</summary><label>Задание<textarea value={prompt} maxLength={4000} onChange={e=>setPrompt(e.target.value)}/></label><div className="message-actions">{Object.entries({generate:'Создать',rewrite:'Переписать',shorten:'Сократить',expand:'Расширить',selling:'Более продающий',formal:'Официальнее',friendly:'Дружелюбнее',correct:'Исправить ошибки',add_emoji:'Добавить эмодзи',remove_emoji:'Убрать эмодзи',variant:'Другой вариант'}).map(([v,l])=><button key={v} disabled={busy} onClick={()=>void ai(v)}>{l}</button>)}</div></details><h3>Кнопки</h3><p>В VK кнопки публикуются как ссылки в тексте.</p>{buttons.map((b,i)=><div key={i}><label>Название<input value={b.text} maxLength={50} onChange={e=>{setButtons(buttons.map((x,j)=>j===i?{...x,text:e.target.value}:x));key.current='';}}/></label><label>HTTPS-ссылка<input type="url" value={b.url} onChange={e=>{setButtons(buttons.map((x,j)=>j===i?{...x,url:e.target.value}:x));key.current='';}}/></label><button onClick={()=>{setButtons(buttons.filter((_,j)=>j!==i));key.current='';}}>Убрать кнопку</button></div>)}<button disabled={buttons.length>=10} onClick={()=>setButtons([...buttons,{text:'',url:''}])}>Добавить кнопку</button><label>Дата<input type="date" value={date} onChange={e=>{setDate(e.target.value);key.current='';}}/></label><label>Время<input type="time" value={time} onChange={e=>{setTime(e.target.value);key.current='';}}/></label><label>Повторение<select value={frequency} onChange={e=>{setFrequency(e.target.value);key.current='';}}>{[['','Без повторения'],['daily','Ежедневно'],['weekdays','Выбранные дни'],['weekly','Еженедельно'],['monthly','Ежемесячно']].map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>{frequency==='weekdays'&&['Вс','Пн','Вт','Ср','Чт','Пт','Сб'].map((d,i)=><label key={i}><input type="checkbox" checked={weekdays.includes(i)} onChange={e=>setWeekdays(e.target.checked?[...weekdays,i]:weekdays.filter(x=>x!==i))}/>{d}</label>)}{frequency&&<label>Окончание повторений<input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label>}<div className="message-actions"><button className="button button--outline" disabled={busy||uploading} onClick={()=>void save('draft')}>Сохранить черновик</button><button className="button button--primary" disabled={busy||uploading} onClick={()=>void save('schedule')}>Запланировать</button><button className="button button--primary" disabled={busy||uploading} onClick={()=>void save('now')}>Опубликовать сейчас</button>{editing&&<button onClick={reset}>Закончить редактирование</button>}</div><details><summary>Подключить площадку</summary><label>Платформа<select value={platform} onChange={e=>setPlatform(e.target.value)}><option value="telegram">Telegram</option><option value="vk">VK</option></select></label>{platform==='telegram'?<label>Канал @username или ID<input value={channel} onChange={e=>setChannel(e.target.value)}/></label>:<label>Пользовательский ключ VK для публикации<input type="password" autoComplete="off" value={publishToken} onChange={e=>setPublishToken(e.target.value)}/></label>}<button disabled={busy} onClick={()=>void connect()}>Проверить и подключить</button></details></section><section className="panel crm-panel"><h2>История публикаций</h2><label>Фильтр<select value={filter} onChange={e=>setFilter(e.target.value)}>{Object.entries({all:'Все',draft:'Черновики',scheduled:'Запланированные',published:'Опубликованные',failed:'Ошибки'}).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>{!loaded?<p>Загрузка…</p>:!posts.length?<p>Публикаций пока нет.</p>:posts.filter(p=>filter==='all'||p.status===filter||filter==='failed'&&p.status==='partial'||filter==='scheduled'&&p.schedule?.active).map(p=><article className="message-bubble" key={p.id}><p>{p.text}</p><strong>{p.schedule?.active?'Повторяющаяся':statuses[p.status]}</strong>{p.scheduled_at&&<p>{new Date(p.scheduled_at).toLocaleString('ru',{timeZone:timezone})}</p>}{p.deliveries.map(d=><p key={d.target_id}>{d.platform} · {d.title}: {statuses[d.status]} {d.last_error?'· '+d.last_error:''}</p>)}<div className="message-actions">{['draft','scheduled'].includes(p.status)&&<button disabled={busy} onClick={()=>{setEditing(p);setText(p.text);setFiles(p.attachments??[]);setChosen(p.deliveries.map(d=>d.target_id));setButtons(p.buttons);setFrequency(p.schedule?.rule.frequency??'');setDate(p.schedule?.rule.start??'');setTime(p.schedule?.rule.time??'12:00');key.current='';}}>Редактировать</button>}<button disabled={busy} onClick={()=>void action(p,'duplicate')}>Дублировать</button>{p.status==='draft'&&<button disabled={busy} onClick={()=>void action(p,'now')}>Опубликовать сейчас</button>}{['failed','partial'].includes(p.status)&&<button disabled={busy} onClick={()=>void action(p,'retry')}>Повторить после ошибки</button>}{['draft','scheduled'].includes(p.status)&&<button disabled={busy} onClick={()=>setConfirm({post:p,action:'cancel'})}>Отменить публикацию</button>}</div></article>)}</section></div>{confirm&&<section className="panel crm-panel" role="dialog" aria-modal="true" aria-label="Отмена публикации"><p>Отменить публикацию и её будущие повторения?</p><button disabled={busy} onClick={()=>void action(confirm.post,confirm.action)}>Да, отменить</button><button onClick={()=>setConfirm(null)}>Назад</button></section>}</div>;
}
