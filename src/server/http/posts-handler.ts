import {getRuntime} from '../runtime';import {createApplication} from './application';import {AppError,json,readJson,requireOrigin,respond} from './errors';import {limit} from './limits';import {PostService} from '../posts/service';
export function postsHandler(request:Request,publicId:string,resource:'posts'|'post-targets',id?:string){return respond(async()=>{const r=getRuntime();const user=await createApplication(r).requireUser(request.headers);const service=new PostService(r.db,r.secret);if(request.method!=='GET'){requireOrigin(request,r.origin);await limit(r.db,r.secret,'posts:'+publicId+':'+user.id,30,60);}if(resource==='post-targets')return json(request.method==='GET'?await service.targets(user.id,publicId):await service.connectTarget(user.id,publicId,await readJson(request,12000)));if(request.method==='GET')return json(await service.list(user.id,publicId));const body=await readJson(request,20000);if(id&&request.method==='POST')return json(await service.action(user.id,publicId,id,body.action));if(request.method==='POST'||request.method==='PATCH')return json(await service.save(user.id,publicId,body,id));throw new AppError(405,'METHOD_NOT_ALLOWED','Действие недоступно.');});}
