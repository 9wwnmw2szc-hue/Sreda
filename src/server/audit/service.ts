import {randomUUID} from 'node:crypto';import type {Transaction} from 'kysely';import type {Database} from '../db/schema.ts';
export async function audit(tx:Transaction<Database>,businessId:string,actor:string|null,action:Database['business_audit_log']['action'],target:string,metadata:Record<string,unknown>={},actorType:'member'|'client'|'system'=actor?'member':'client'){await tx.insertInto('business_audit_log').values({id:randomUUID(),business_id:businessId,actor_user_id:actor,actor_type:actorType,action,target_id:target,target_user_id:null,details:target,metadata:JSON.stringify(metadata)}).execute();}
